server:
  applicationConnectors:
    - type: http
      port: 8080

  requestLog:
    appenders: []

auth: ${WIRE_HOLDEM_SERVICE_TOKEN}   # Service's auth token

swagger:
  resourcePackage: com.wire.bots.sdk.server.resources
  uriPrefix: /holdem
  schemes:
    - http
    - https

db:
  host: localhost
  port: 6379